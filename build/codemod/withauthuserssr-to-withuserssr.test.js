import path from"path";import jscodeshift from"jscodeshift";import transform from"codemod/withauthuserssr-to-withuserssr";import readFile from"codemod/util/readFile";function read(r){return readFile(path.join(__dirname,r))}const transformName="withauthuserssr-to-withuserssr";describe("withAuthUserSSR -> withUserSSR",(()=>{it("modifies the API name",(()=>{const r=transform({source:read(`./${transformName}.fixtures/inputA.js`),path:require.resolve(`./${transformName}.fixtures/inputA.js`)},{jscodeshift},{}),t=read(`./${transformName}.fixtures/outputA.js`);expect(r).toEqual(t)}))}));