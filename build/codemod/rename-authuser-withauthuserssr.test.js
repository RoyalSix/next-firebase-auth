import path from"path";import jscodeshift from"jscodeshift";import transform from"codemod/rename-authuser-withauthuserssr";import readFile from"codemod/util/readFile";function read(r){return readFile(path.join(__dirname,r))}const transformName="rename-authuser-withauthuserssr";describe("withAuthUserSSR argument property change: AuthUser -> user",(()=>{it("works as expected",(()=>{const r=transform({source:read(`./${transformName}.fixtures/inputA.js`),path:require.resolve(`./${transformName}.fixtures/inputA.js`)},{jscodeshift},{}),e=read(`./${transformName}.fixtures/outputA.js`);expect(r).toEqual(e)}))}));