import path from"path";import jscodeshift from"jscodeshift";import transform from"codemod/useauthuser-to-useuser";import readFile from"codemod/util/readFile";function read(e){return readFile(path.join(__dirname,e))}const transformName="useauthuser-to-useuser";describe("useAuthUser -> useUser",(()=>{it("modifies the API name",(()=>{const e=transform({source:read(`./${transformName}.fixtures/inputA.js`),path:require.resolve(`./${transformName}.fixtures/inputA.js`)},{jscodeshift},{}),r=read(`./${transformName}.fixtures/outputA.js`);expect(e).toEqual(r)}))}));