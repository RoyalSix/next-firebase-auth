import path from"path";import jscodeshift from"jscodeshift";import transform from"codemod/withauthusertokenssr-to-withusertokenssr";import readFile from"codemod/util/readFile";function read(t){return readFile(path.join(__dirname,t))}const transformName="withauthusertokenssr-to-withusertokenssr";describe("withAuthUserTokenSSR -> withUserTokenSSR",(()=>{it("modifies the API name",(()=>{const t=transform({source:read(`./${transformName}.fixtures/inputA.js`),path:require.resolve(`./${transformName}.fixtures/inputA.js`)},{jscodeshift},{}),e=read(`./${transformName}.fixtures/outputA.js`);expect(t).toEqual(e)}))}));